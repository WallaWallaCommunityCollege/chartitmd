<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:views="clr-namespace:ChartItMD.Views"
            x:Class="ChartItMD.Views.MainPage"
                  Title="ChartItMD">

    <MasterDetailPage.Master>

        <views:MenuPage Title="ChartItMD" />
        



    </MasterDetailPage.Master>
    <MasterDetailPage.ToolbarItems>
        <ToolbarItem Text="{Binding PatientName}"/>
        <ToolbarItem Text="{Binding PatientDOB}"/>
        <ToolbarItem Text="{Binding PatientAge}"/>
        <ToolbarItem Text="{Binding PatientGender}"/>
        <ToolbarItem Text="{Binding PatientID}"/>



    </MasterDetailPage.ToolbarItems>

    <MasterDetailPage.Detail>
        <NavigationPage>
            <NavigationPage.Icon>
                <OnPlatform x:TypeArguments="FileImageSource">
                    <On Platform="iOS" Value="tab_feed.png"/>
                </OnPlatform>
            </NavigationPage.Icon>
            <x:Arguments>
                <views:ItemsPage />
            </x:Arguments>
        </NavigationPage>
    </MasterDetailPage.Detail>
</MasterDetailPage>